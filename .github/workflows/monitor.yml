name: Webook Monitor

on:
  schedule:
    # تشغيل كل 5 دقائق على مدار 24 ساعة طوال أيام الأسبوع
    - cron: '*/5 * * * *'
  workflow_dispatch: # يتيح لك التشغيل اليدوي في أي وقت

jobs:
  check-webook:
    runs-on: ubuntu-latest
    steps:
    - name: Check out repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Restore cache for seen events
      uses: actions/cache@v4
      with:
        path: seen_events.json
        key: seen-events-${{ github.run_id }}
        restore-keys: |
          seen-events-

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install playwright requests
        playwright install chromium

    - name: Run Webook monitor script
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: python main.py

    - name: Save cache for seen events
      uses: actions/cache@v4
      with:
        path: seen_events.json
        key: seen-events-${{ github.run_id }}
